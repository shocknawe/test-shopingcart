syntax = "proto3";

package transaction;

option go_package = "/transaction";

message Product {
  // SKU used in examples
  // 120P90
  string sku = 1;
  string name = 2;

  // price times 1000
  int64 price = 3;

  int32 inventory_quantity = 4;
}
